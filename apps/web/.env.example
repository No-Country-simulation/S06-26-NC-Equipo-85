# App BiT — environment variables
# Copy to .env.local for development. Never commit real secrets.

# Public API base URL (exposed to browser, must be a valid URL)
NEXT_PUBLIC_API_URL=http://localhost:port

NODE_ENV=development

# Set to "1" to skip env validation (useful in Docker build stage)
# SKIP_ENV_VALIDATION=1
